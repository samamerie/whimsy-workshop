/* Whimsy Workshop — page settings (e.g. the About photo). Edit via admin.html
   (About photo tab), then Download settings.js and replace this file to go live. */
window.WW_SETTINGS = { "aboutImage": "" };
